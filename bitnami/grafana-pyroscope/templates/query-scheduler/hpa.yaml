{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.queryScheduler.enabled .Values.queryScheduler.autoscaling.hpa.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "grafana-pyroscope.query-scheduler.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: grafana-pyroscope
    app.kubernetes.io/component: query-scheduler
  {{- if or .Values.commonAnnotations .Values.queryScheduler.autoscaling.hpa.annotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.queryScheduler.autoscaling.hpa.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ template "grafana-pyroscope.query-scheduler.fullname" . }}
  minReplicas: {{ .Values.queryScheduler.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.queryScheduler.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.queryScheduler.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.queryScheduler.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.queryScheduler.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.queryScheduler.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
