{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.adhocProfiles.enabled .Values.adhocProfiles.autoscaling.hpa.enabled }}
apiVersion: {{ include "common.capabilities.hpa.apiVersion" ( dict "context" $ ) }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "grafana-pyroscope.ad-hoc-profiles.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: grafana-pyroscope
    app.kubernetes.io/component: ad-hoc-profiles
  {{- if or .Values.commonAnnotations .Values.adhocProfiles.autoscaling.hpa.annotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.adhocProfiles.autoscaling.hpa.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: {{ include "common.capabilities.deployment.apiVersion" . }}
    kind: Deployment
    name: {{ template "grafana-pyroscope.ad-hoc-profiles.fullname" . }}
  minReplicas: {{ .Values.adhocProfiles.autoscaling.hpa.minReplicas }}
  maxReplicas: {{ .Values.adhocProfiles.autoscaling.hpa.maxReplicas }}
  metrics:
    {{- if .Values.adhocProfiles.autoscaling.hpa.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adhocProfiles.autoscaling.hpa.targetCPU }}
    {{- end }}
    {{- if .Values.adhocProfiles.autoscaling.hpa.targetMemory }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.adhocProfiles.autoscaling.hpa.targetMemory }}
    {{- end }}
{{- end }}
